# Lunchify client — copy to .env and fill in. .env is git-ignored.
# These are baked into the static build; production uses the public HTTPS URL.

VITE_KEYCLOAK_URL=http://localhost:8081
VITE_KEYCLOAK_REALM=azultech
VITE_KEYCLOAK_CLIENT_ID=lunchify
